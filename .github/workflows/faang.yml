# Adapted from ChatGPT: 
# https://chatgpt.com/share/69418c36-5610-8011-8488-c1d25c6f2bba

name: Run faang.py and push changes

on:
  workflow_dispatch:
  schedule:
    # Reference: https://crontab.guru/#24_10_*_*_6
    - cron: "24 10 * * 3"
    
permissions: 
  contents: write  

jobs:
  run-faang-script:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0 
          
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt || true

      - name: Run faang.py
        run: |
          python faang.py

      - name: Commit changes
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add data plots
          git commit -m "Automated FAANG data update" || echo "No changes to commit"

      - name: Push changes
        run: |
          git push origin HEAD
